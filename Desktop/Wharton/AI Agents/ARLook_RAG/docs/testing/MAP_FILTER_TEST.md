# 地图价格筛选测试指南

## 🧪 测试步骤

### 1. 验证数据加载
1. 打开浏览器控制台（F12）
2. 刷新页面
3. 查看控制台日志，确认所有属性已加载
4. 查找日志：`✅ Loaded all properties: X`

### 2. 测试价格筛选
1. 在地图左上角找到价格滑块
2. 将价格设置为 $2500
3. 查看控制台日志：
   - `💰 Filtered by maxPrice $2500: X properties`
   - `✅ Final filtered result: X properties`
   - 确认 The Accolade ($2150) 在列表中

### 3. 预期结果

#### maxPrice = $2500
- ✅ **The Accolade** ($2150) - 应该显示
- ❌ Arrive University City ($3084) - 不应该显示
- ❌ The Chestnut ($3645) - 不应该显示
- ❌ 3737 Chestnut ($3900) - 不应该显示
- ❌ Domus ($3231) - 不应该显示

#### maxPrice = $2400
- ✅ **The Accolade** ($2150) - 应该显示

#### maxPrice = $3000
- ✅ **The Accolade** ($2150) - 应该显示
- ✅ Arrive University City ($3084) - 不应该显示（因为 > 3000）
- ❌ The Chestnut ($3645) - 不应该显示
- ❌ 3737 Chestnut ($3900) - 不应该显示
- ❌ Domus ($3231) - 不应该显示

#### maxPrice = $3200
- ✅ **The Accolade** ($2150) - 应该显示
- ✅ Arrive University City ($3084) - 应该显示
- ❌ The Chestnut ($3645) - 不应该显示
- ❌ 3737 Chestnut ($3900) - 不应该显示
- ❌ Domus ($3231) - 不应该显示

## 🔍 调试信息

### 控制台日志检查点

1. **数据加载阶段**:
   ```
   ✅ Loaded all properties: X
   ```

2. **筛选阶段**:
   ```
   📊 Filtering X total properties
   💰 Filtered by maxPrice $X: Y properties
   ✅ Final filtered result: Y properties
   ```

3. **地图标记更新**:
   ```
   📍 Updating markers: { markerCount: X, filters: {...} }
   📊 Filtered markers: X out of Y
   ✅ Added X markers to map
   ```

### 常见问题排查

#### 问题1: The Accolade 不显示
**检查**:
- [ ] 控制台日志显示价格解析是否正确 ($2150)
- [ ] 控制台日志显示筛选后的属性列表包含 The Accolade
- [ ] 地图标记数量是否匹配筛选后的属性数量

#### 问题2: 价格解析错误
**检查**:
- [ ] CSV文件中的价格格式是否正确：`$1,900–$2,400`
- [ ] `parsePriceRange` 函数是否正确解析

#### 问题3: 地图不更新
**检查**:
- [ ] `handleMapFiltersUpdate` 是否被调用
- [ ] `setProperties` 是否更新了状态
- [ ] `MapContainer` 组件的 `markers` prop 是否更新

## 📝 测试用例

运行测试脚本：
```bash
npx tsx scripts/test-price-filter.ts
```

这将验证：
- ✅ 价格解析函数
- ✅ 筛选逻辑

## 🐛 报告问题

如果发现问题，请提供：
1. 设置的价格筛选值
2. 控制台完整日志
3. 预期显示但未显示的属性名称
4. 实际显示的属性列表

