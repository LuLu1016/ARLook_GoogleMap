# Google Maps API Key 获取详细步骤

## 📋 完整步骤指南

### 步骤 1: 访问 Google Cloud Console

1. 打开浏览器，访问：**https://console.cloud.google.com/**
2. 使用您的Google账号登录（如果没有账号，需要先注册）

### 步骤 2: 创建新项目

1. 点击页面顶部的**项目选择器**（显示当前项目名称或"选择项目"）
2. 点击弹窗中的**"新建项目"**按钮
3. 填写项目信息：
   - **项目名称**: `ARLook`（或您喜欢的名称）
   - **组织**: 保持默认（如果适用）
   - **位置**: 保持默认
4. 点击**"创建"**按钮
5. 等待几秒钟，项目创建完成后会自动选择该项目

### 步骤 3: 启用 Maps JavaScript API

1. 在左侧菜单中，点击 **"API和服务"** > **"库"**
   - 或者直接访问：https://console.cloud.google.com/apis/library
2. 在搜索框中输入：**`Maps JavaScript API`**
3. 点击搜索结果中的 **"Maps JavaScript API"**
4. 在详情页面，点击**"启用"**按钮
5. 等待几秒钟，API启用成功后会出现"API已启用"的提示

### 步骤 4: 创建 API Key

1. 在左侧菜单中，点击 **"API和服务"** > **"凭据"**
   - 或者直接访问：https://console.cloud.google.com/apis/credentials
2. 点击页面顶部的 **"创建凭据"** 按钮
3. 在下拉菜单中选择 **"API密钥"**
4. 系统会立即生成一个API密钥，格式类似：`AIzaSyAbc123def456ghi789jkl012mno345pqr`
5. **重要**：复制这个API密钥（点击复制按钮或手动复制）

### 步骤 5: 配置 API Key 限制（强烈推荐）

为了安全性和防止滥用，建议配置API Key限制：

#### 5.1 打开API密钥设置
1. 在"凭据"页面，点击刚创建的API密钥名称
2. 进入API密钥详情页面

#### 5.2 设置应用限制
1. 找到 **"应用限制"** 部分
2. 选择 **"HTTP引荐来源网址（网站）"**
3. 点击 **"添加项目"** 按钮
4. 添加以下网址（每行一个）：
   ```
   http://localhost:3000/*
   http://localhost:3000
   http://127.0.0.1:3000/*
   ```
5. 点击 **"完成"**

#### 5.3 设置API限制
1. 找到 **"API限制"** 部分
2. 选择 **"限制密钥"**
3. 在下方勾选 **"Maps JavaScript API"**
4. （可选）取消勾选其他不需要的API
5. 点击 **"保存"** 按钮

### 步骤 6: 复制API密钥

1. 在API密钥详情页面，您可以看到完整的API密钥
2. 点击 **"复制"** 图标或手动选择并复制整个密钥
3. API密钥格式示例：`AIzaSyAbc123def456ghi789jkl012mno345pqr`

### 步骤 7: 更新项目配置

#### 方法A: 使用命令行（推荐）

```bash
cd "/Users/lulu/Desktop/Wharton/AI Agents/ARLook_RAG"
```

然后运行：
```bash
read -p "请输入您的 Google Maps API Key: " api_key
echo "NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=${api_key}" > .env.local
echo "OPENAI_API_KEY=your_openai_key_here" >> .env.local
echo "✅ API Key 已保存！"
```

#### 方法B: 手动编辑文件

1. 打开项目根目录的 `.env.local` 文件
2. 找到这一行：
   ```
   NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your_api_key_here
   ```
3. 将 `your_api_key_here` 替换为您的实际API密钥：
   ```
   NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=AIzaSyAbc123def456ghi789jkl012mno345pqr
   ```
4. 保存文件

### 步骤 8: 重启开发服务器

1. 在运行 `npm run dev` 的终端窗口中，按 **`Ctrl+C`** 停止服务器
2. 重新启动：
   ```bash
   npm run dev
   ```
3. 等待服务器启动完成（看到 "Ready" 提示）
4. 刷新浏览器页面：**http://localhost:3000**

## ✅ 验证设置

如果设置成功，您应该看到：
- ✅ 地图正常显示（费城大学城区域）
- ✅ 显示5个房源标记点（蓝色圆圈）
- ✅ 可以点击标记点查看房源详情
- ✅ 地图与聊天联动正常

## 🔒 安全提醒

**重要**：已配置的限制设置很重要！

- ✅ **应用限制**：只允许 localhost 访问，防止API Key被滥用
- ✅ **API限制**：只允许 Maps JavaScript API，防止其他API被调用产生费用
- ✅ **环境变量**：API Key存储在 `.env.local`，不会提交到Git仓库

## 💰 费用说明

Google Maps Platform提供：
- **每月$200免费额度**（足够开发和小规模使用）
- 超过免费额度后按使用量付费
- 详细定价：https://developers.google.com/maps/billing-and-pricing/pricing

## 🆘 常见问题

### Q1: 找不到"API和服务"菜单？
**A**: 确保您已经创建了项目并选择了正确的项目。项目选择器在页面顶部。

### Q2: 找不到"Maps JavaScript API"？
**A**: 
- 确认您已登录Google Cloud Console
- 尝试刷新页面
- 直接在搜索框输入完整名称

### Q3: API Key创建后立即看不到？
**A**: 
- 刷新页面
- 检查是否选择了正确的项目
- 在"凭据"页面查看所有API密钥

### Q4: 配置限制后地图不显示？
**A**: 
- 确认HTTP引荐来源网址限制中包含了 `http://localhost:3000/*`
- 确认API限制中勾选了"Maps JavaScript API"
- 检查浏览器控制台是否有错误信息

### Q5: 如何查看API使用情况？
**A**: 
- 在Google Cloud Console中，进入"API和服务" > "仪表板"
- 可以查看各个API的使用量和费用

## 📚 参考链接

- [Google Maps Platform文档](https://developers.google.com/maps/documentation/javascript/get-api-key)
- [API密钥安全最佳实践](https://developers.google.com/maps/api-security-best-practices)
- [定价信息](https://developers.google.com/maps/billing-and-pricing/pricing)

## 🎯 快速检查清单

完成以下步骤后，您就可以使用Google Maps了：

- [ ] 创建了Google Cloud项目
- [ ] 启用了Maps JavaScript API
- [ ] 创建了API密钥
- [ ] 配置了应用限制（HTTP引荐来源网址）
- [ ] 配置了API限制（只允许Maps JavaScript API）
- [ ] 复制了API密钥
- [ ] 更新了.env.local文件
- [ ] 重启了开发服务器
- [ ] 验证地图正常显示

祝您设置顺利！🚀

